<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exambro.overlay">

    <!-- Permission wajib untuk overlay -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Wajib untuk Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!--
        FIX BUG 1: Wajib di Android 14 (targetSdk 34).
        Tanpa ini, startForeground() crash → overlay tidak pernah muncul.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Exambro"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExambroOverlay">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            FIX BUG 1: Tambah android:foregroundServiceType="specialUse"
            WAJIB untuk targetSdk 34 (Android 14+).
            Tanpa ini startForeground() throw ForegroundServiceStartNotAllowedException
            → service crash → overlay tidak muncul.
        -->
        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

    </application>

</manifest>